# v0.8 Human Edit Verification Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the v0.8 acceptance loop for reviewing table recommendations and verifying human edits.

**Architecture:** Add `igess.verification` as the focused service module. Keep `igess.cli` thin. Reuse existing loader, linter, builder, simulator, output writer, compare, gate, and Luban exporter utilities.

**Tech Stack:** Python 3.11+, pytest, stdlib JSON/pathlib/decimal/hashlib, existing IGESS services.

---

## Chunk 1: Proposal Review

### Task 1: Normalize current advice recommendations

**Files:**
- Create: `tests/test_verification.py`
- Create: `src/igess/verification.py`

- [x] **Step 1: Write failing test**

Verify `review_proposal()` reads `table_recommendations` from `advice.json`.

- [x] **Step 2: Run RED**

Command:

```powershell
E:/IGESS/.venv/Scripts/python.exe -m pytest tests/test_verification.py::test_review_proposal_normalizes_advice_recommendations -v
```

Observed: failed because `igess.verification` did not exist.

- [x] **Step 3: Implement minimal review service**

Added proposal loading, normalization, JSON writing, and Markdown rendering.

- [x] **Step 4: Run GREEN**

Observed: test passed.

### Task 2: Normalize future tuning report changes

**Files:**
- Modify: `tests/test_verification.py`
- Modify: `src/igess/verification.py`

- [x] **Step 1: Write failing test**

Verify `best_candidates[*].changes` are extracted and retain `candidate_id`.

- [x] **Step 2: Run RED**

Observed: failed with `proposal contains no table recommendations`.

- [x] **Step 3: Implement tuning extraction**

Extended extraction for `best_candidates[*].changes`.

- [x] **Step 4: Run GREEN**

Observed: test passed.

## Chunk 2: Edit Verification

### Task 3: Verify exported table values

**Files:**
- Modify: `tests/test_verification.py`
- Modify: `src/igess/verification.py`

- [x] **Step 1: Write failing tests**

Covered exact match, numeric range match, descriptive suggestion, and mismatch.

- [x] **Step 2: Run RED**

Observed: failed because `verify_edits` did not exist.

- [x] **Step 3: Implement verification service**

Added table JSON lookup, exact/range/descriptive matching, rollup status,
simulation run, JSON/Markdown reports, optional `--datas` export, compare, and
gate support.

- [x] **Step 4: Run GREEN**

Observed: parameterized verification tests passed.

### Task 4: Protect source workbooks

**Files:**
- Modify: `tests/test_verification.py`

- [x] **Step 1: Add safety test**

Hash `data-tables/Datas/generators.xlsx`, run `verify_edits(..., datas=...)`,
and assert the hash is unchanged.

- [x] **Step 2: Run GREEN**

Observed: test passed and exported JSON appeared under `verify/exported_tables`.

## Chunk 3: CLI and Docs

### Task 5: Add CLI commands

**Files:**
- Modify: `src/igess/cli.py`
- Modify: `tests/test_verification.py`

- [x] **Step 1: Write CLI tests**

Covered `review-proposal` and `verify-edits`.

- [x] **Step 2: Run RED**

Observed: argparse rejected unknown commands.

- [x] **Step 3: Implement CLI branches**

Registered command arguments and delegated to `review_proposal()` /
`verify_edits()`.

- [x] **Step 4: Run GREEN**

Observed: CLI tests passed.

### Task 6: Update README

**Files:**
- Modify: `README.md`

- [x] **Step 1: Add v0.8 usage section**

Documented proposal review, exported-table verification, and source workbook
verification.

- [x] **Step 2: Run docs tests**

Observed: `tests/test_docs_examples.py` passed.

## Final Verification

- [x] `E:/IGESS/.venv/Scripts/python.exe -m pytest tests/test_verification.py -v`
- [x] `E:/IGESS/.venv/Scripts/python.exe -m pytest`
- [x] CLI smoke:
  - `advise`
  - `review-proposal`
  - `verify-edits`
