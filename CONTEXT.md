# IGESS

IGESS 用于构建、运行和评估可由数据表驱动的游戏数值模型。

## Language

**黑盒验证与数值调优**：
执行者在不接触项目源码的前提下，通过修改和导出数据表、运行测试版本、复现异常并提交结构化报告，验证游戏行为和调整数值；不包含定位代码根因或修复程序缺陷。
_Avoid_: 调试、代码调试

**源码隔离**：
通过交付物和访问权限防止执行者在正常工作中接触或误泄露源码，但不以抵抗其主动逆向本地程序为安全目标。
_Avoid_: 源码防破解、绝对保密

**执行工具包**：
交付给执行策划、可在其电脑上独立运行的裁剪版 IGESS，包含黑盒验证与数值调优所需能力，但不包含源码、版本库历史、测试代码或内部设计文档。
_Avoid_: 源码包、开发环境

**IGESS 运行时**：
执行工具包中承载 IGESS 行为的编译产物或字节码，不包含可直接阅读的 `.py` 源文件；它与执行策划可自行安装的 Python 和公开第三方依赖相区分。
_Avoid_: IGESS Python 源码、源码 wheel

**导表快照**：
由执行策划通过既有导表流程生成、作为执行工具包唯一数值输入的一组 JSON 文件；执行工具包对其绝对只读，不读取或生成 Excel/Luban 源表，所有输出均进入运行历史库。
_Avoid_: 源表、Excel 表

**依赖范围校验**：
只校验本次所选场景实际读取的表、记录、字段和引用；未被读取的数据缺失、额外内容或版本差异不阻塞运行，实际依赖的数据缺失或无效则终止运行并产生业务诊断。
_Avoid_: 全表校验、整包版本校验

**运行快照**：
执行策划启动任务时一次性读入内存并计算文件哈希的场景依赖数据；运行期间磁盘导表快照的变化只影响下一次任务，运行历史默认不永久复制其完整 JSON。
_Avoid_: 实时导表目录、持久数值副本

**分发仓库**：
与 IGESS 源码仓库隔离、供执行策划拉取和更新执行工具包的 Git 仓库，仅承载生成后的交付物，不承载执行策划的数值源表或数值变更。
_Avoid_: 源码仓库、开发仓库

**发布候选**：
由源码仓库导出到空目录、通过交付白名单和源码泄露检查，但尚未经所有者人工确认并提交到分发仓库的执行工具包。
_Avoid_: 正式发布、自动发布

**调优工作台**：
执行策划使用执行工具包的唯一操作入口，通过浏览器选择导表快照和预设场景、启动验证、查看报告、对比历史结果、查看固定回归检查并导出诊断包，不要求使用命令行，也不暴露模型编辑和开发诊断能力。
_Avoid_: CLI、开发控制台

**业务诊断**：
调优工作台向执行策划展示的可操作错误，定位到导表快照中的文件、记录和字段，但不包含 IGESS 的原始 traceback、源码路径或源码片段。
_Avoid_: traceback、内部异常

**诊断包**：
执行策划在工具内部异常时交给 IGESS 所有者的排错材料，默认包含错误编号、工具版本、输入文件指纹和清洗后的日志，不包含 IGESS 源码信息；只有执行策划明确选择时才附带完整导表快照。
_Avoid_: 崩溃转储、源码日志

**运行历史库**：
位于 Windows 用户本地数据目录、由调优工作台管理的模拟结果和报告集合，与分发仓库和执行策划的数值源表仓库相互隔离；工具不得自动删除记录，用户可以按次删除或手动清空。
_Avoid_: Git 工作区输出目录、分发仓库历史

**比较基线**：
与候选结果由同一执行工具包版本生成、用于判断数值变化影响的历史运行；旧版本报告仍可查看，但不能作为新版本结果的比较基线。
_Avoid_: 跨版本基线、旧版对照

**调优报告**：
黑盒验证与数值调优的核心成果，呈现模拟指标、趋势、对比和回归结论；可按需要展示游戏经济公式与场景假设，但不得泄露 IGESS 的实现源码信息。
_Avoid_: 调试日志、源码报告

**Fish ID（鱼配置身份）**：
一条鱼配置的稳定身份，由稀有度前缀和该稀有度内的非零槽位组成；新增鱼占用未使用槽位，不要求连续，也不重编号已有鱼。
_Avoid_: Fish 行号、Fish 数组下标

**Fish 稀有度**：
鱼配置中显式声明的分类，必须与 Fish ID 的稀有度前缀一致，但不是从 Fish ID 静默推导出的替代字段。
_Avoid_: Fish ID 排序等级

**Fish 概率阈值（Denominator）**：
决定鱼概率选择顺序的阈值，与 Fish ID 和导表数组顺序相互独立。
_Avoid_: Fish ID 阈值、导表顺序阈值
