# 金币升级代码审查

基准：`10bd0587b7e35482a4a0f4b48d434189dc2e5291`。
实现提交：`5a1f509`；命令：`git diff 10bd0587b7e35482a4a0f4b48d434189dc2e5291...HEAD`。
按 code-review 技能由两个独立子代理并行审查；最后补充的杠铃策略测试也通过 Spec 审查。

## Standards

**明确规范违反：未发现。** 本次变更遵循本地 `.scratch/<feature>/spec.md` 布局、
规范状态标签及 `CONTEXT.md` 的鱼升级／鱼品质术语；未发现与两份执行工具包分发 ADR 冲突的改动。

**判断性建议（非阻塞）：**

- **Feature Envy — `src/igess/fish_behavior_targets.py`**
  新增片段：
  ```python
  gain = (hall_adapter.income_trace(item).income_per_second * SimNumber.parse("0.25")
          * hall.trash_man_rebirth_multiplier * reward_multiplier)
  ```
  行为策略自行组合鱼厅增长公式和转世倍率，需了解鱼厅内部收益规则。建议让
  `FishHallDataAdapter` 提供升级后的收入增量，策略仅应用画像倍率并比较等待时间。
  这可避免鱼厅收益规则变化时同步维护第二处计算。当前普通鱼、已上阵及固定
  1.25 成长条件下，此简式成立；不据此判定现有数值错误。

其余改动未发现值得单独报告的基线坏味道。Standards 轴仅静态审查。

处理：作为后续模块边界改进建议记录；本轮现有普通鱼规则下计算等价，未扩大为通用阵容预演模块。

## Spec

未发现需要修复的具体问题。

- **缺失或部分实现**：未发现。金币扣款、品质前 X 与上阵交集、最低等级优先、
  杠铃资金条件、神兽排除、checkpoint、紧凑账本及报告均符合规格。
- **范围扩大**：未发现。
- **已实现但行为错误**：未发现。当前鱼厅固定采用最大收益阵容，已上阵目标升级的
  25% 单鱼增量等价于鱼厅实际增量，且已计入永久与画像倍率。

验证：定向测试 **18 passed**；smoke、一天、一周、一月运行状态均为 `success`，
报告存在，适用范围及模型摘要一致。

既有 `model status` 通用探针问题未计入本次发现。付费画像尚无独立月基线已明确披露；
规格未要求两画像各自重建月基线。

结论：Standards 1 项非阻塞建议（最严重：增收公式归属）；Spec 0 项发现。
